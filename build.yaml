# Build matrix for GitHub Actions.
#
# The Waveshare RP2040-Zero is USB-only, so there is a single firmware image
# (no left/right, no settings_reset — to wipe an RP2040 you flash
# flash_nuke.uf2 instead).
---
include:
  - board: rp2040_zero
    shield: mono34
    artifact-name: mono34
